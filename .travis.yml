language: node_js
node_js:
  - "stable"
notifications:
  email: false

cache:
  derictories:
  - node_modules

script:
  npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
